C TEST READING ASCII IMAGE FILE
	PROGRAM READIMG
	  IMPLICIT NONE
	  CHARACTER IFNAME*64
	  INTEGER I,J,B
	  INTEGER FIELDS(500)
	  INTEGER ARR(500,320)
	  CHARACTER*2000 LINE
	  IFNAME='mariner_mercury.asc'
	  OPEN(11,FILE=IFNAME,FORM='FORMATTED')
	  DO 10,J=1,320
	      READ(11,'(A)',END=999) LINE
	      READ(LINE,*) FIELDS
      DO 40, I=1,500
        ARR(I,J)=FIELDS(I)
40    CONTINUE
10	  CONTINUE
	  CLOSE(11)
      DO 60,J=1,320
        DO 50,I=1,500
          PRINT *,J,I,ARR(I,J) 
50      CONTINUE
60    CONTINUE
100	FORMAT(500(A))
999     CONTINUE
	END
